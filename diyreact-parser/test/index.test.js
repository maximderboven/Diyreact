// class declaration shoul be supported by the parser
test('class declaration should be supported by the parser', () => {
    const input = `
    class App extends Component {
        render() {
            return (
                <div>
                    <h1>Hello World</h1>
                </div>
            )
        }
    }
    `
    // const ast
    // expect(ast).toEqual({}
} )

